[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --tb=short --strict-markers -rNs
markers =
    fontbakery: invokes the external fontbakery tool (slow)
