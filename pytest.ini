[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --tb=short --strict-markers -rNs --ignore=tests/test_fontspector.py
markers =
    fontspector: invokes the external fontspector tool (slow)
